## Knowledge Distillery Batch Report — 2026-03-24 (batch 2)

### Summary
| Metric | Value |
|--------|-------|
| Source PRs processed | 4 |
| Candidates extracted | 6 |
| Accepted (fact / anti-pattern) | 3 (3 / 0) |
| Rejected | 3 |
| Insufficient evidence (deferred) | 0 |
| Archived via curation | 3 |

### Accepted Entries

| ID | Type | Title | Domains | Source PR |
|----|------|-------|---------|-----------|
| `record-decision-trigger-taxonomy` | fact | Record-decision trigger taxonomy: what counts as a recordable decision | knowledge-distillery | #11 |
| `pr-template-enforcement-out-of-scope` | fact | PR body template enforcement is out of scope for knowledge-distillery | knowledge-distillery | #11 |
| `archive-rejected-vault-entries` | fact | Archive rejected vault entries rather than deleting | cli, pipeline | #10 |

### Archived Entries (Rejected via Curation)

| ID | Type | Title | Reason |
|----|------|-------|--------|
| `decision-commits-skip-memento-note` | fact | Decision commits do not require a memento note | 구현 디테일 수준으로 vault에 넣을 만큼의 가치가 없음 |
| `curate-workflow-branch-prefix-gate` | fact | Restrict /curate trigger to knowledge/batch-* branches only | 워크플로우 트리거 조건은 코드에서 자명한 구현 디테일 |
| `slash-command-trigger-case-sensitive` | fact | The /curate slash command trigger is intentionally case-sensitive | 컨벤션에 따른 당연한 동작으로 별도 지식으로 관리할 필요 없음 |

### Rejected Candidates

| Source PR | Code | Reason |
|-----------|------|--------|
| #8 | R1_EVIDENCE_INSUFFICIENT | Guard `::add-mask::` calls with non-empty check — single-developer PR with zero human reviews, no explicit team discussion |
| #8 | R1_EVIDENCE_INSUFFICIENT | Upgrade actions/checkout to v6 for Node.js 24 support — single-developer PR, no review comments or formal approvals |
| #8 | R1_EVIDENCE_INSUFFICIENT | Remove FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 workaround — single-developer cleanup, no review, implementation detail |

### Curation Queue (Human Review Required)

No conflicts detected.

### Domain Changes

**New domains auto-created during this batch:** None (all entries mapped to pre-existing domains: `knowledge-distillery`, `cli`, `pipeline`).

**Domain health highlights (from `domain-report`):**

- `schema` (1 entry) and `cli` (2 entries) remain merge candidates — consider consolidating into `plugin-infra` once more entries accumulate.
- `pipeline` grew from 1 → 3 entries this batch (now reduced to 1 active entry after curation archived 2 pipeline entries).
- `knowledge-distillery` domain (new this batch): absorbed 3 entries on decision-recording workflow knowledge, 2 of which remain active after curation.
- `.knowledge/reports/` still has no domain mapping (structural mismatch — was flagged in previous batch, still unresolved).

**Uncovered path prefixes observed across this batch:**

- `.knowledge/decisions/` — PR #11 introduced the first committed decision file (`.knowledge/decisions/2026-03-24-pr-template-out-of-scope.md`); no domain pattern covers this prefix. Suggested follow-up: add pattern to `knowledge-distillery` domain or create a dedicated `decisions` domain.
- `AGENTS.md` (root) — PR #11 modified root-level `AGENTS.md`; no domain covers root directive files. Low priority — this is project configuration, not code.
- `.github/workflows/` (PR #8) — already covered by `pipeline` domain; no action needed.

**Manual follow-up suggestions:**
- Add `.knowledge/decisions/` pattern to `knowledge-distillery` or `pipeline` domain.
- Evaluate merging `schema` and `cli` into `plugin-infra` after a few more batches confirm the pattern holds.

### Source PR Details

- #9 "knowledge: batch 2026-03-24 — 3 entries added": 0 candidates — this is an auto-generated batch report PR (meta-artifact). The PR body is pipeline output, not team discussion. No extractable knowledge signals.
- #8 "Fix Node.js 20 deprecation and empty secret mask in workflows": 3 candidates extracted, all rejected (R1_EVIDENCE_INSUFFICIENT). Single-developer PR with no reviews; fixes are real and correct but no explicit team agreement is visible in evidence.
- #11 "Add record-decision skill for auto-capturing project decisions": 3 candidates extracted, all accepted initially; 1 archived via curation (`decision-commits-skip-memento-note`). 2 remain active.
- #10 "Add Report PR curation via /curate comment command": 3 candidates extracted, all accepted initially; 2 archived via curation (`curate-workflow-branch-prefix-gate`, `slash-command-trigger-case-sensitive`). 1 remains active.

### Insufficient Evidence (Remains Pending)

All PRs had sufficient evidence.

### Curation Log

| Action | Entry ID | Details | Timestamp |
|--------|----------|---------|-----------|
| Archived | `decision-commits-skip-memento-note` | Reason: 구현 디테일 수준으로 vault에 넣을 만큼의 가치가 없음 | 2026-03-24T08:43:30Z |
| Archived | `curate-workflow-branch-prefix-gate` | Reason: 워크플로우 트리거 조건은 코드에서 자명한 구현 디테일 | 2026-03-24T08:43:39Z |
| Archived | `slash-command-trigger-case-sensitive` | Reason: 컨벤션에 따른 당연한 동작으로 별도 지식으로 관리할 필요 없음 | 2026-03-24T08:43:42Z |

---

### How to Curate This Report

This PR contains new knowledge entries already inserted into `vault.db`. You can selectively accept, reject, or modify entries before merging.

**To provide feedback:**
1. Leave comments on this PR referencing entry IDs from the Accepted Entries table:
   - Reject: "Reject `entry-id` — reason"
   - Modify: "Change the claim of `entry-id` to: new text"
   - Update domains: "Move `entry-id` to domain `new-domain`"
2. Post a comment with **`/curate`** to trigger automated processing
3. Review the updated diff after curation completes
4. Merge when satisfied, or run `/curate` again for further changes

**What `/curate` does:**
- Rejected entries are archived in vault.db (preserves history, not deleted)
- Modified entries are updated in-place
- The batch report is regenerated to reflect current state
- A summary comment is posted with all actions taken
