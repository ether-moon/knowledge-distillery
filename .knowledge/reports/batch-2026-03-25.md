# Knowledge Distillery Batch Report — 2026-03-25

### Summary
| Metric | Value |
|--------|-------|
| Source PRs processed | 7 |
| Candidates extracted | 6 |
| Accepted (fact / anti-pattern) | 2 (1 / 1) |
| Rejected | 4 (2 via quality gate, 4 via curation) |
| Insufficient evidence (deferred) | 0 |
| Pipeline artifacts skipped | 1 |

### Accepted Entries

| ID | Type | Title | Domains | Source PR |
|----|------|-------|---------|-----------|
| `memento-commit-no-glob-staging` | anti-pattern | MUST NOT use git add -A in memento-commit | skills | #13 |
| `vault-changeset-not-direct-db` | fact | Write vault entries to changeset JSON, not vault.db directly | pipeline, cli, skills | #17 |

### Rejected Candidates

No candidates were rejected through the quality gate. PRs where no candidates were extracted are noted in Source PR Details below.

### Rejected Entries (via Curation)

| ID | Type | Title | Reason |
|----|------|-------|--------|
| `record-decision-isolated-commit` | fact | Use git commit --only in record-decision to isolate the decision file | Reviewer requested to keep only memento-commit-no-glob-staging and vault-changeset-not-direct-db; all other entries removed. |
| `vault-hook-reminder-not-auto-query` | fact | Vault UserPromptSubmit hook must only remind, not auto-query | Reviewer requested to keep only memento-commit-no-glob-staging and vault-changeset-not-direct-db; all other entries removed. |
| `pre-commit-blocker-over-postcommit-reminder` | fact | Use PreToolUse blocker for commit enforcement, not PostToolUse reminder | Reviewer requested to keep only memento-commit-no-glob-staging and vault-changeset-not-direct-db; all other entries removed. |
| `mark-evidence-skip-batch-branches` | fact | mark-evidence workflow must skip knowledge/batch-* branch PRs | Reviewer requested to keep only memento-commit-no-glob-staging and vault-changeset-not-direct-db; all other entries removed. |

### Curation Queue (Human Review Required)

_Resolved via curation — no open items._

### Domain Changes

**No new domains proposed in this batch.** All entries mapped to pre-existing domains: `skills`, `knowledge-distillery`, `plugin-infra`, `pipeline`, `cli`.

**Unmapped path prefixes observed across this batch:**
- `plugins/knowledge-distillery/hooks/` — appears in PRs #14, #15. No domain pattern currently covers this prefix. The hooks directory is part of the plugin infrastructure; consider adding a `plugins/knowledge-distillery/hooks/` pattern to the `plugin-infra` or `skills` domain.
- `AGENTS.md` (root) — appears across several PRs. No domain covers root-level directive files.
- `.claude/settings.json` — appears in PR #14. No domain mapping.

**Manual follow-up suggestions:**
- Add `plugins/knowledge-distillery/hooks/` pattern to `plugin-infra` domain (hooks enforce plugin-level behavioral requirements).
- Evaluate whether to add `AGENTS.md` to an existing domain (e.g., `knowledge-distillery` or a new `directives` domain).

### Source PR Details

- #12 "knowledge: batch 2026-03-24 (2) — 3 entries added": **Skipped** — pipeline-generated Report PR (knowledge/batch-* branch). mark-evidence should not run on these per the rule established in PR #18. 0 candidates extracted.
- #13 "Improve all plugin skills: frontmatter, descriptions, and safety fixes": 2 candidates extracted; `memento-commit-no-glob-staging` accepted, `record-decision-isolated-commit` rejected via curation.
- #14 "Add UserPromptSubmit hook for Knowledge Vault query reminder": 1 candidate extracted; `vault-hook-reminder-not-auto-query` rejected via curation.
- #15 "Replace PostToolUse memento reminder with PreToolUse commit blocker": 1 candidate extracted; `pre-commit-blocker-over-postcommit-reminder` rejected via curation.
- #16 "Fix curate-report workflow to skip instead of fail on guard conditions": 0 candidates extracted. The change (guard conditions exit as skip, not failure) was assessed as a workflow implementation detail derivable from reading the workflow code. No extractable knowledge-grade candidates.
- #17 "Replace vault.db binary commits with changeset flow": 1 candidate extracted; `vault-changeset-not-direct-db` accepted.
- #18 "Skip mark-evidence for pipeline-generated batch PRs": 1 candidate extracted; `mark-evidence-skip-batch-branches` rejected via curation.

### Insufficient Evidence (Remains Pending)

All PRs had sufficient evidence.

---

### Curation Log
| Action | Entry ID | Details | Timestamp |
|--------|----------|---------|-----------|
| Rejected | `record-decision-isolated-commit` | Reviewer requested to keep only memento-commit-no-glob-staging and vault-changeset-not-direct-db | 2026-03-25T05:36:40Z |
| Rejected | `vault-hook-reminder-not-auto-query` | Reviewer requested to keep only memento-commit-no-glob-staging and vault-changeset-not-direct-db | 2026-03-25T05:36:40Z |
| Rejected | `pre-commit-blocker-over-postcommit-reminder` | Reviewer requested to keep only memento-commit-no-glob-staging and vault-changeset-not-direct-db | 2026-03-25T05:36:40Z |
| Rejected | `mark-evidence-skip-batch-branches` | Reviewer requested to keep only memento-commit-no-glob-staging and vault-changeset-not-direct-db | 2026-03-25T05:36:40Z |

---

### How to Curate This Report

This PR contains a **changeset** with new knowledge entry candidates. Entries are **not yet in vault.db** — they will be applied automatically when this PR is merged.

**To provide feedback:**
1. Leave comments on this PR referencing entry IDs from the Accepted Entries table:
   - Reject: "Reject `entry-id` — reason"
   - Modify: "Change the claim of `entry-id` to: new text"
   - Update domains: "Move `entry-id` to domain `new-domain`"
2. Post a comment with **`/curate`** to trigger automated processing
3. Review the updated changeset after curation completes
4. Merge when satisfied, or run `/curate` again for further changes

**What `/curate` does:**
- Rejected entries are marked as `rejected` in the changeset (excluded from vault insertion)
- Modified entries are updated in the changeset
- The batch report is regenerated to reflect current state
- A summary comment is posted with all actions taken

**What happens on merge:**
- A post-merge workflow applies the changeset to vault.db on main
- Only entries with `status: "accepted"` are inserted
