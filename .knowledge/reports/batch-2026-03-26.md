# Knowledge Distillery Batch Report — 2026-03-26

## Summary

| Metric | Value |
|--------|-------|
| Source PRs processed | 2 |
| Candidates extracted | 6 |
| Accepted (fact / anti-pattern) | 2 (2 / 0) |
| Rejected | 4 |
| Insufficient evidence (deferred) | 0 |

## Accepted Entries

| ID | Type | Title | Domains | Source PR |
|----|------|-------|---------|-----------|
| `push-notes-ref-after-commit` | fact | Push notes ref to remote after memento note attachment | skills | #20 |
| `domain-names-prioritize-trigger-quality` | fact | Domain names must prioritize trigger quality over brevity | skills, docs, pipeline | #21 |

## Rejected Entries (via Curation)

| ID | Type | Title | Reject Reason |
|----|------|-------|---------------|
| `memento-commit-mandatory-over-bare-git` | fact | Use memento-commit skill for every project commit | Already codified in CLAUDE.md/AGENTS.md Memento section and auto-memory. Directly derivable from current artifacts with no residual value. |
| `domain-list-ids-only-lightweight-index` | fact | Use domain-list --ids-only for lightweight domain index preload | Describes a CLI feature directly derivable from code and --help output. No residual value beyond what reading the implementation provides. |
| `refinement-reuses-domain-registry` | fact | Refinement must consult domain registry before proposing new domains | Procedure fully codified in extract-candidates SKILL.md checklist items 10-12. Directly derivable from current artifacts. |
| `domain-maintenance-annotation-structured` | fact | Capture domain registry improvement signals as _domain_maintenance annotations | Schema and contract fully documented in extract-candidates SKILL.md _domain_maintenance section. Directly derivable from current artifacts. |

## Curation Queue (Human Review Required)

No conflicts detected.

## Domain Changes

No new domains are proposed in this batch. All entries map to existing domains.

**Current domain registry baseline (7 domains):**

| Domain | Description | Active Entries |
|--------|-------------|----------------|
| `cli` | knowledge-gate CLI tool — the sole runtime access path to the vault | 3 |
| `docs` | Design documents, adoption guide, and project documentation | 0 |
| `knowledge-distillery` | auto-created by pipeline | 2 |
| `pipeline` | Distillation pipeline orchestration — GitHub Actions workflows and stage coordination | 3 |
| `plugin-infra` | Plugin packaging — marketplace.json, plugin.json, directory structure | 4 |
| `schema` | SQLite vault schema, migrations, and data model | 1 |
| `skills` | Claude Code plugin skills — pipeline stages and agent-facing skills | 5 |

**Domain maintenance observations from this batch:**

None. All candidates reused existing domains cleanly. No near-duplicates or scope issues detected.

**Unmapped path prefixes observed:**

- `tests/` — test scripts for CLI and prompt contracts appear in PR #21 changed files but have no domain mapping. Consider a `testing` or `test-infrastructure` domain if test-specific knowledge accumulates in future batches.
- `.knowledge/decisions/` — decision records appear across both PRs but resolve to no specific domain (likely caught by global domain). May warrant a `decision-records` domain if decision-specific rules emerge.

## Source PR Details

- #20 "Fix git sessions count always showing 0 in evidence bundle": 2 candidates extracted, 1 accepted (1 rejected via curation — already in CLAUDE.md). Root cause was missing `git push origin refs/notes/commits` in memento-commit skill.
- #21 "Refine domain discovery guidance and contracts": 4 candidates extracted, 1 accepted (3 rejected via curation — implementation details derivable from current artifacts). Domain naming quality standard preserved as cross-cutting design rationale.

## Insufficient Evidence (Remains Pending)

All PRs had sufficient evidence.

## Curation Log

| Action | Entry ID | Details | Timestamp |
|--------|----------|---------|-----------|
| Rejected | `memento-commit-mandatory-over-bare-git` | Already in CLAUDE.md/AGENTS.md; directly derivable (R7) | 2026-03-26 |
| Rejected | `domain-list-ids-only-lightweight-index` | CLI feature derivable from code; no residual value (R7) | 2026-03-26 |
| Rejected | `refinement-reuses-domain-registry` | Codified in skill checklist items 10-12; directly derivable (R7) | 2026-03-26 |
| Rejected | `domain-maintenance-annotation-structured` | Schema documented in skill file; directly derivable (R7) | 2026-03-26 |
| Updated | `push-notes-ref-after-commit` | Claim and body modified: stripped implementation details, focused on failure mode and silent CI breakage | 2026-03-26 |
| Kept | `domain-names-prioritize-trigger-quality` | Cross-cutting design rationale not derivable from single artifact | 2026-03-26 |

---

### How to Curate This Report

This PR contains a **changeset** with new knowledge entry candidates. Entries are **not yet in vault.db** — they will be applied automatically when this PR is merged.

**To provide feedback:**
1. Leave comments on this PR referencing entry IDs from the Accepted Entries table:
   - Reject: "Reject `entry-id` — reason"
   - Modify: "Change the claim of `entry-id` to: new text"
   - Update domains: "Move `entry-id` to domain `new-domain`"
2. Post a comment with **`/curate`** to trigger automated processing
3. Review the updated changeset after curation completes
4. Merge when satisfied, or run `/curate` again for further changes

**What `/curate` does:**
- Rejected entries are marked as `rejected` in the changeset (excluded from vault insertion)
- Modified entries are updated in the changeset
- The batch report is regenerated to reflect current state
- A summary comment is posted with all actions taken

**What happens on merge:**
- A post-merge workflow applies the changeset to vault.db on main
- Only entries with `status: "accepted"` are inserted
