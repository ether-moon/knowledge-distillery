# Knowledge Distillery Batch Report — 2026-03-30

## Summary

| Metric | Value |
|--------|-------|
| Source PRs processed | 2 |
| Candidates extracted | 8 |
| Accepted (fact / anti-pattern) | 0 (0 / 0) |
| Rejected via curation | 8 |
| Insufficient evidence (deferred) | 0 |

## Accepted Entries

No entries accepted in this batch. All 8 candidates were rejected via curation (see below).

## Rejected Entries (via Curation)

| ID | Type | Claim (summary) | Reject Reason |
|----|------|-----------------|---------------|
| `apply-changeset-branch-regex` | fact | MUST store `head.ref` in env var and validate against regex | Q2=no — shell injection prevention is pattern-inherent rationale; env var + regex validation purpose is self-evident from the pattern itself |
| `curate-report-author-association-gate` | fact | MUST gate `/curate` on `author_association` allowlist | Q2=no — standard GitHub Actions access control pattern; OWNER/MEMBER/COLLABORATOR allowlist is standard practice, self-evident from the implementation |
| `mcp-json-must-be-gitignored` | fact | `.mcp.json` MUST be listed in `.gitignore` | Q2=no — gitignoring runtime config files is standard engineering practice; already present in .gitignore |
| `knowledge-gate-summary-index-default` | fact | All query commands MUST return lightweight summary index by default | Q2=no — progressive disclosure token savings is self-evident from the pattern itself; documented in CLI code and AGENTS.md |
| `summary-index-claim-field-required` | fact | Summary index MUST include `claim` field | Q2=no — necessity of including core assertion in summary index is self-evident from design intent; output format is implemented in code |
| `get-many-fail-fast-missing-ids` | fact | `get-many` MUST exit 1 with error listing missing IDs | Q2=no — fail-fast preventing silent failure is pattern-inherent; error message already conveys the intent |
| `query-unknown-flags-rejected` | fact | Query commands MUST reject unknown flags with usage error | Q2=no — standard CLI error handling; catch-all error message already explains the intent |
| `ids-only-opt-in-not-default` | fact | `--ids-only` MUST remain opt-in | Q2=no — extra round-trip required with IDs-only is self-evident from the two-mode design; opt-in nature is clear from flag design |

## Source PR Details

- #27 "fix: harden workflow templates against injection and unauthorized access": 3 candidates extracted, 0 accepted, 3 rejected via curation (Q2=no — all three hardening patterns are standard practice or pattern-inherent).
- #28 "feat: progressive disclosure and get-many batch query for knowledge-gate": 5 candidates extracted, 0 accepted, 5 rejected via curation (Q2=no — all five CLI design decisions are self-evident from the two-mode output design and standard CLI conventions).

## Insufficient Evidence (Remains Pending)

All PRs had sufficient evidence.

---

### Curation Log

| Action | Entry ID | Details | Timestamp |
|--------|----------|---------|-----------|
| Rejected | `apply-changeset-branch-regex` | Q2=no — shell injection prevention is pattern-inherent; env var + regex purpose self-evident | 2026-03-30T10:26:51Z |
| Rejected | `curate-report-author-association-gate` | Q2=no — standard GitHub Actions access control; OWNER/MEMBER/COLLABORATOR allowlist is standard practice | 2026-03-30T10:26:51Z |
| Rejected | `mcp-json-must-be-gitignored` | Q2=no — gitignoring runtime config is standard engineering practice | 2026-03-30T10:26:51Z |
| Rejected | `knowledge-gate-summary-index-default` | Q2=no — progressive disclosure token savings self-evident from pattern; documented in code and AGENTS.md | 2026-03-30T10:26:51Z |
| Rejected | `summary-index-claim-field-required` | Q2=no — claim inclusion necessity self-evident from design intent; format implemented in code | 2026-03-30T10:26:51Z |
| Rejected | `get-many-fail-fast-missing-ids` | Q2=no — fail-fast preventing silent failure is pattern-inherent; error message conveys intent | 2026-03-30T10:26:51Z |
| Rejected | `query-unknown-flags-rejected` | Q2=no — standard CLI error handling; error message explains intent | 2026-03-30T10:26:51Z |
| Rejected | `ids-only-opt-in-not-default` | Q2=no — extra round-trip with IDs-only self-evident from two-mode design | 2026-03-30T10:26:51Z |

---

### How to Curate This Report

This PR contains a **changeset** with new knowledge entry candidates. Entries are **not yet in vault.db** — they will be applied automatically when this PR is merged.

**To provide feedback:**
1. Leave comments on this PR referencing entry IDs from the Accepted Entries table:
   - Reject: "Reject `entry-id` — reason"
   - Modify: "Change the claim of `entry-id` to: new text"
   - Update domains: "Move `entry-id` to domain `new-domain`"
2. Post a comment with **`/curate`** to trigger automated processing
3. Review the updated changeset after curation completes
4. Merge when satisfied, or run `/curate` again for further changes

**What `/curate` does:**
- Rejected entries are marked as `rejected` in the changeset (excluded from vault insertion)
- Modified entries are updated in the changeset
- The batch report is regenerated to reflect current state
- A summary comment is posted with all actions taken

**What happens on merge:**
- A post-merge workflow applies the changeset to vault.db on main
- Only entries with `status: "accepted"` are inserted
