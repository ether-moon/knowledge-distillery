## Knowledge Distillery Batch Report — 2026-04-27

### 진행 상황

| 항목 | 상태 |
|------|------|
| #48 | ✅ 처리 완료 (0 accepted, run #24988294638) |
| #49 | ✅ 처리 완료 (0 accepted, run #24988294638) |
| #50 | ✅ 처리 완료 (0 accepted, run #24988294638) |

### Summary
| Metric | Value |
|--------|-------|
| Source PRs processed | 3 |
| Candidates extracted | 0 |
| Accepted (fact / anti-pattern) | 0 (0 / 0) |
| Rejected | 0 |
| Insufficient evidence (deferred) | 0 |

### Accepted Entries

(None)

### Rejected Candidates

(None)

### Curation Queue (Human Review Required)

No conflicts detected.

### Domain Changes

**New domains proposed in this batch:**

없음 — 이번 배치에서 새 도메인 제안 없음.

**Domain-to-path mapping status for this batch:**

| 변경 경로 | 도메인 |
|-----------|--------|
| `.github/workflows/batch-refine.yml` | `pipeline` |
| `plugins/knowledge-distillery/skills/batch-refine/SKILL.md` | `skills` |
| `plugins/knowledge-distillery/skills/collect-evidence/SKILL.md` | `skills` |
| `.claude/settings.json` | ❌ 미매핑 |
| `AGENTS.md` | ❌ 미매핑 |
| `tests/handoff-structure.sh` | ❌ 미매핑 |
| `tests/fixtures/orchestration/batch-refine.json` | ❌ 미매핑 |
| `tests/lib/handoff-structure.sh` | ❌ 미매핑 |

**반복 미매핑 경로 접두사:**
- `tests/` — 이번 배치의 PR #48에서 4개 파일이 수정됨. 현재 아무 도메인에도 매핑되지 않음. 테스트 파일에 대한 도메인(예: `pipeline-testing` 또는 기존 `pipeline` 도메인 패턴 확장)을 검토 권장.
- `.claude/` — PR #49에서 `settings.json`과 삭제된 `skills/bumping-version/SKILL.md` 포함. `plugin-infra` 도메인이 이 경로를 커버해야 할 수 있음.
- `AGENTS.md` (루트 레벨) — 프로젝트 지시 파일. 도메인 매핑 없음.

**후속 조치 제안:**
- `tests/` 경로를 `pipeline` 또는 신규 `pipeline-testing` 도메인에 매핑 검토
- `.claude/settings.json` 및 `.claude/skills/` 경로를 `plugin-infra` 도메인 패턴에 추가 검토
- `AGENTS.md`, `CLAUDE.md` 등 루트 지시 파일 매핑 정책 결정 (예: 전역 `*` 도메인에 포함되어 있으면 충분한지 여부)

### Vault Feedback (Existing Entry Signals)

No feedback on existing entries.

### Insufficient Evidence (Remains Pending)

All PRs had sufficient evidence.

---

### How to Curate This Report

This PR contains a **changeset** with new knowledge entry candidates. Entries are **not yet in vault.db** — they will be applied automatically when this PR is merged.

**To provide feedback:**
1. Leave comments on this PR referencing entry IDs from the Accepted Entries list:
   - Reject: "Reject `entry-id` — reason"
   - Modify: "Change the claim of `entry-id` to: new text"
   - Update domains: "Move `entry-id` to domain `new-domain`"
2. Post a comment with **`/curate`** to trigger automated processing
3. Review the updated changeset after curation completes
4. Merge when satisfied, or run `/curate` again for further changes

**What `/curate` does:**
- Rejected entries are marked as `rejected` in the changeset (excluded from vault insertion)
- Modified entries are updated in the changeset
- The batch report is regenerated to reflect current state
- A summary comment is posted with all actions taken

**What happens on merge:**
- A post-merge workflow applies the changeset to vault.db on main
- Only entries with `status: "accepted"` are inserted
