## Knowledge Distillery Batch Report — 2026-03-24

### Summary
| Metric | Value |
|--------|-------|
| Source PRs processed | 2 |
| Candidates extracted | 3 |
| Accepted (fact / anti-pattern) | 3 (3 / 0) |
| Rejected | 0 |
| Insufficient evidence (deferred) | 0 |

### Accepted Entries

| ID | Type | Title | Domains | Source PR |
|----|------|-------|---------|-----------|
| `dual-enforcement-agent-behavioral-requirements` | fact | Dual enforcement for agent behavioral requirements | plugin-infra, skills | #7 |
| `combine-commit-note-single-shell-call` | fact | Combine commit and note attachment in single shell call | plugin-infra, skills | #7 |
| `detect-directive-delegation-before-placing-sections` | fact | Detect directive delegation pattern before placing sections | skills, plugin-infra | #7 |

### Rejected Candidates

_No candidates rejected._

### Curation Queue (Human Review Required)

No conflicts detected.

### Domain Changes

**No new domains auto-created during this batch.**

**Domain health highlights (from `domain-report`):**

- `cli`, `schema`, `pipeline` are merge candidates (each has only 1 entry). No action taken — domain reorganization is a manual follow-up.
- `.knowledge/reports/` path prefix has no domain mapping (structural mismatch surfaced by domain-report).

**Uncovered path prefixes observed across this batch:**

- `plugins/knowledge-distillery/hooks/` — PR #7 introduced `hooks/post-commit-memento.sh` and `hooks/hooks.json`; no domain mapping exists for this path prefix. Suggested follow-up: create a `hooks` domain or add this pattern to `plugin-infra`.
- `AGENTS.md`, `CLAUDE.md` (root directive files) — no domain covers root-level directive files. Low priority, as these are project-level configuration rather than code artifacts.

**Manual follow-up suggestions:**
- Consider adding `plugins/knowledge-distillery/hooks/` pattern to the `plugin-infra` or `skills` domain.
- Evaluate merging `cli`, `schema`, and `pipeline` domains once each has accumulated more entries, or merge into `plugin-infra` if content warrants.

### Source PR Details

- #6 "test: Stage A debug": zero candidates — debug-only PR with no code changes, no discussion, no memento notes. Evidence collected successfully but no knowledge extractable.
- #7 "Add memento-commit skill with PostToolUse hook": 3 candidates extracted and accepted — architectural decisions on dual enforcement pattern, hook/commit atomicity, and directive file placement detection.

### Insufficient Evidence (Remains Pending)

All PRs had sufficient evidence.
