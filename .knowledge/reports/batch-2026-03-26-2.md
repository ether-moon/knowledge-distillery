# Knowledge Distillery Batch Report — 2026-03-26 (run 2)

## Summary

| Metric | Value |
|--------|-------|
| Source PRs processed | 3 |
| Candidates extracted | 5 |
| Accepted (fact / anti-pattern) | 4 (4 / 0) |
| Rejected | 2 |
| Insufficient evidence (deferred) | 0 |

## Accepted Entries

| ID | Type | Title | Domains | Source PR |
|----|------|-------|---------|-----------|
| `derivability-two-question-test` | fact | Two-question derivability test for extraction criterion 4d | skills | #23 |
| `git-history-not-derivability-reject` | fact | Git log/blame traceability is not a rejection reason for vault candidates | skills | #23 |
| `tests-must-be-shell-jq-based` | fact | Tests must remain shell/jq-based; no Python test dependency | pipeline | #24 |
| `interactive-cli-smoke-test-only` | fact | Interactive CLI flows can only be smoke-tested in automation | cli | #24 |

## Rejected Candidates

| Source PR | Candidate ID | Code | Reason |
|-----------|-------------|------|--------|
| #23 | `fact-type-state-description-skip` | R7_DIRECTLY_DERIVABLE | Rule fully visible in current SKILL.md files (extract-candidates criterion 4d 'Fact-type filter' and quality-gate R7 'Fact-type heuristic' sections). Motivation captured in derivability-two-question-test candidate. No residual value. |
| #24 | `fixture-driven-orchestration-structure-split` | R7_DIRECTLY_DERIVABLE | The separation of structure vs. orchestration test files is directly visible from the file naming convention introduced in this PR. Q1=yes (derivable from file layout), Q2=no (no non-obvious rationale). |

## Curation Queue (Human Review Required)

- `derivability-two-question-test` <-> `conservative-extraction-principle`: The existing `conservative-extraction-principle` entry lists '(4) not self-evident' as one of the extraction criteria in its body. The new entry replaces that criterion with the Q1/Q2 test, making the existing body partially stale. Consider updating `conservative-extraction-principle` body to reference the Q1/Q2 test.

## Domain Changes

No new domains proposed in this batch. All accepted entries mapped to existing domains (`skills`, `pipeline`, `cli`).

**Current domain registry baseline (7 domains):**

| Domain | Active Entries |
|--------|----------------|
| `cli` | 3 |
| `docs` | 0 |
| `knowledge-distillery` | 2 |
| `pipeline` | 3 |
| `plugin-infra` | 4 |
| `schema` | 1 |
| `skills` | 5 |

**Domain maintenance observations from this batch:**

None. All candidates reused existing domains cleanly.

**Unmapped path prefixes observed:**

- `tests/` — test scripts continue to appear across PRs #24 and #25 with no dedicated domain. A `test-infrastructure` domain may be warranted when test-specific knowledge accumulates further (previously flagged in batch-2026-03-26 report).

## Source PR Details

- #23 "Strengthen codified knowledge filtering in refine pipeline": 3 candidates extracted, 2 accepted, 1 rejected (R7 — fact-type filter rule derivable from SKILL.md, motivation subsumed by accepted `derivability-two-question-test` entry). Core change was replacing the abstract 'not self-evident' criterion with the Q1/Q2 derivability + residual value decision table.
- #24 "test: add regression harnesses for skills and knowledge-gate": 3 candidates extracted, 2 accepted, 1 rejected (R7 — fixture split pattern self-evident from file layout). Established shell/jq-only testing constraint and interactive CLI smoke-test limitation.
- #25 "test: harden skill orchestration harness validation": 0 candidates extracted. Changes were narrowly scoped shell test fixes (set -e safety guard, single-key validation) — both patterns directly derivable from reading the harness code.

## Insufficient Evidence (Remains Pending)

All PRs had sufficient evidence.

---

### How to Curate This Report

This PR contains a **changeset** with new knowledge entry candidates. Entries are **not yet in vault.db** — they will be applied automatically when this PR is merged.

**To provide feedback:**
1. Leave comments on this PR referencing entry IDs from the Accepted Entries table:
   - Reject: "Reject `entry-id` — reason"
   - Modify: "Change the claim of `entry-id` to: new text"
   - Update domains: "Move `entry-id` to domain `new-domain`"
2. Post a comment with **`/curate`** to trigger automated processing
3. Review the updated changeset after curation completes
4. Merge when satisfied, or run `/curate` again for further changes

**What `/curate` does:**
- Rejected entries are marked as `rejected` in the changeset (excluded from vault insertion)
- Modified entries are updated in the changeset
- The batch report is regenerated to reflect current state
- A summary comment is posted with all actions taken

**What happens on merge:**
- A post-merge workflow applies the changeset to vault.db on main
- Only entries with `status: "accepted"` are inserted
