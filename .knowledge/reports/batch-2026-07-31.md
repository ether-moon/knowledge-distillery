## Knowledge Distillery Batch Report — 2026-07-31

### 진행 상황

| 항목 | 상태 |
|------|------|
| #55 | ✅ 처리 완료 (0 accepted, 1m56s, run #30627573891) |

### Summary
| Metric | Value |
|--------|-------|
| Source PRs processed | 1 |
| Candidates extracted | 1 |
| Accepted (fact / anti-pattern) | 0 (0 / 0) |
| Rejected | 1 |
| Insufficient evidence (deferred) | 0 |
| 총 소요시간(wall-clock) | 2m0s (run #30627573891) |

### 운영 Metric (Triage)

이번 batch 기준 누적값입니다. 추세 추적용입니다.

| Metric | 값 |
|--------|----|
| Layer 1 skip 수 | 0 (bot-dependency-update=0, lockfile-only=0, generated-only=0, auto-revert=0, docs-only=0, i18n-only=0) |
| Layer 2 skip 수 | 0 |
| Layer 2 defer 수 | 0 |
| Layer 2 extract 수 | 4 |
| `knowledge:pending` 큐 길이 (batch 시작 시) | 1 |
| `knowledge:skipped` 누적 PR 수 (전체) | 0 |
| 최근 positive-recall regression rate (수동 backtest) | 10.0% |

### Accepted Entries

No entries accepted in this batch.

### Rejected Candidates

#### @ether-moon
- #55
  - [SCHEMA_INVALID] `shared-changeset-validator-before-merge` — Proposed fact about running the shared `_changeset-validate` Layer 1 validator at every changeset persistence boundary (pre-merge CI, batch-refine, curate-report, changeset-apply); rejected because the `claim` field exceeded the 200-character schema limit, so Layer 2 semantic checks were short-circuited and skipped.

### Curation Queue (Human Review Required)

No conflicts detected.

### Domain Changes

**New domains proposed in this batch:**
None. The rejected candidate referenced existing registry domains: `pipeline`, `skills`.

No domain maintenance findings, near-duplicate domain candidates, or repeated unmapped path prefixes were observed in this batch.

### Vault Feedback (Existing Entry Signals)

No feedback on existing entries.

### Insufficient Evidence (Remains Pending)

All PRs had sufficient evidence.

<!-- KD_BATCH_PR_META {"pr_number":55,"changed_files":[".github/workflows/apply-changeset.yml","plugins/knowledge-distillery/skills/batch-refine/SKILL.md","plugins/knowledge-distillery/skills/curate-report/SKILL.md","plugins/knowledge-distillery/skills/knowledge-gate/scripts/knowledge-gate","plugins/knowledge-distillery/skills/setup/SKILL.md","plugins/knowledge-distillery/skills/setup/assets/workflows/apply-changeset.yml","tests/batch-refine-structure.sh","tests/knowledge-gate.sh","tests/lib/batch-refine-structure.sh","tests/skill-packaging.sh"]} -->

### Deferred Queue (Human Curation Required)

triage가 `defer` 판정을 내린 PR입니다. 사람이 라벨을 변경해야 다음 batch에 반영됩니다.

| PR | 작성자 | 제목 | Defer 사유 |
|----|--------|------|------------|

No deferred PRs.

**사람 검토 가이드:**
- 지식 가치가 있다고 판단 → 라벨을 `knowledge:deferred` → `knowledge:pending` 으로 변경.
- 영구 제외 → 라벨을 `knowledge:deferred` → `knowledge:skipped` 로 변경. 사유를 PR 코멘트에 남기는 것을 권장.

---

### How to Curate This Report

This PR contains a **changeset** with new knowledge entry candidates. Entries are **not yet in vault.db** — they will be applied automatically when this PR is merged.

**To provide feedback:**
1. Leave comments on this PR referencing entry IDs from the Accepted Entries list:
   - Reject: "Reject `entry-id` — reason"
   - Modify: "Change the claim of `entry-id` to: new text"
   - Update domains: "Move `entry-id` to domain `new-domain`"
2. Post a comment with **`/curate`** to trigger automated processing
3. Review the updated changeset after curation completes
4. Confirm the `validate-changeset` check passes
5. Merge when satisfied, or run `/curate` again for further changes

**What `/curate` does:**
- Rejected entries are marked as `rejected` in the changeset (excluded from vault insertion)
- Modified entries are updated in the changeset
- The batch report is regenerated to reflect current state
- A summary comment is posted with all actions taken

**What happens on merge:**
- A post-merge workflow applies the changeset to vault.db on main
- Only entries with `status: "accepted"` are inserted
